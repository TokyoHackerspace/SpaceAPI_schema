Be sure to do the following when updating the yaml file:

1: you have to build and deploy it using githubs pages build tool

2: validate the schema FIRST using the SpaceAPI validator: https://spaceapi.io/validator/
